export function SellerHomePage() {
  return (
    <div className="max-w-2xl">
      <h2 className="text-2xl font-semibold mb-2">Your listings & orders</h2>
      <p className="text-muted-foreground">
        Seller dashboard arrives in a later phase — the backend endpoints are
        still being built.
      </p>
    </div>
  );
}
